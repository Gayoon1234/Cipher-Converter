<input type="input" class="form-control" placeholder="number" min=0, max=9999 id="input" oninput="myFunction()">
<canvas class="canvasImage" id="myCanvas" width="600" height="300"
style="border:1px">
Your browser does not support the canvas element.
</canvas>
<script>
function myFunction() {
   draw_cistercian(document.getElementById("input").value);
}

function draw_cistercian(letters) {

   // num = parseInt(number);
    var canvas = document.getElementById("myCanvas");
    var ctx = canvas.getContext("2d");

    canvas.style = "position: absolute; top: 0px; left: 0px; right: 0px; bottom: 0px; margin: auto; border:2px solid #c3c3c3";


    ctx.clearRect(0, 0, canvas.width, canvas.height);

    if(true){
        ctx.beginPath();
        // ctx.moveTo(100,0);
        // ctx.lineTo(100,300);

        for(i = 0; i<letters.length;i++){
            drawShape(letters[i],i)
        }

        ctx.closePath();
        ctx.stroke();
    }
}

function drawShape(letter, i ){
    var canvas = document.getElementById("myCanvas");
    var ctx = canvas.getContext("2d");

    if(i >= 60){
        console.log("First row filled")
    }else{
        ctx.moveTo((i%6)*100+50,0 + 100*Math.floor((i/6)));
        ctx.lineTo((i%6)*100+50,100+100*Math.floor((i/6)));

        console.log(100*Math.floor((i/6.1)-1));
    }
}
</script>
